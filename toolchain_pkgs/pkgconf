ver=1.6.3

[srcs]
http://distfiles.dereferenced.org/%name/%name-%ver.tar.xz

[build]
LDFLAGS="-static" \
./configure \
	--prefix=%tools
%make
make install

cp pkgconf %tools/bin/pkg-config
cp pkgconf %tools/bin/$TARGET-pkg-config
cp pkgconf %tools/bin/$TARGET-pkgconf
