ver=2.3.1

[srcs]
https://download-mirror.savannah.gnu.org/releases/%name/%name-%ver.tar.xz

[deps]
attr

[build]
%conf \
	--bindir=/bin \
	--disable-nls
%mk

ln -sf ../lib/$(readlink %dest/lib/libacl.so) %dest/lib/libacl.so
rm -r %dest/share/doc
