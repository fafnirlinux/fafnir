# contrib/rtkit README

## General usage
This port needs rtkit user and group, so run the `post-install` script.
After that, you will need to add your user to the `rtkit` group to be
able to use rtkit.

// vim:filetype=markdown

// End of file
