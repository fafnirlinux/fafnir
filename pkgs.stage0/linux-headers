ver=5.17.7

[srcs]
https://cdn.kernel.org/pub/linux/kernel/v5.x/linux-%ver.tar.xz

[build]
mkdir %src/linux

make \
	ARCH=$ARCH \
	mrproper

mkdir -p %rootfs/usr

make \
	O=%src/linux \
	ARCH=$ARCH \
	INSTALL_HDR_PATH=%rootfs/usr \
	headers_install
