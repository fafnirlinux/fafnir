ver=4.0.2

[srcs]
https://github.com/gavinhoward/bc/archive/%ver.tar.gz::%name-%ver

[build]
sed -i 's|#! /bin/sh|#!/usr/bin/env bash|' configure.sh

PREFIX=%prefix ./configure.sh \
	--disable-nls
%make PREFIX=%prefix
make PREFIX=%prefix DESTDIR=%tools install

rm -rf %tools/share/locale
