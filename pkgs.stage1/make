ver=4.3

[srcs]
https://ftp.gnu.org/gnu/%name/%name-%ver.tar.gz

[build]
./configure \
	--prefix=%prefix \
	--without-guile \
	--host=$XTARGET \
	--build=$(build-aux/config.guess)

%make
make DESTDIR=%rootfs install
