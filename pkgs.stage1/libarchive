ver=3.6.0

[srcs]
https://libarchive.org/downloads/%name-%ver.tar.xz

[build]
%conf \
	--host=$XTARGET \
	--with-sysroot=%rootfs \
	--with-pic \
	--without-openssl \
	--without-xml2 \
	--without-expat \
	--disable-acl \
	--enable-shared \
	--enable-static

%make && make DESTDIR=%rootfs install
