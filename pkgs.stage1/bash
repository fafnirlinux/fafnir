ver=5.1.16

[srcs]
https://ftp.gnu.org/gnu/%name/%name-%ver.tar.gz

[build]
%conf \
	--host=$XTARGET \
	--build=$XHOST \
	--without-bash-malloc \
	--with-curses

make && make DESTDIR=%rootfs install

ln -s bash %rootfs/bin/sh
